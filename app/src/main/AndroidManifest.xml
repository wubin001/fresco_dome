<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.nico.app">


    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/axc_icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.activity.BasicImageActivity"
            android:launchMode="singleTask"
            />
        <activity android:name=".ui.activity.basicimage.Basicone"
            android:launchMode="singleTask"
            />
        <activity android:name=".ui.activity.basicimage.CenterImageActivity"
            android:launchMode="singleTask"/>
        <activity android:name=".ui.activity.basicimage.RoundImageActivity"
            android:launchMode="singleTask"
            />
    </application>

</manifest>